import * as ping from "./ping";
import * as register from "./register";
import * as oath from "./oath";

export const commands = {
    ping, register, oath, // filecase, pendingcases, transferpetition,
    // assigncase, noa, help
};