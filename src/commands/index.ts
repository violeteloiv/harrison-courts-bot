import * as help from "./help";
import * as oath from "./oath";
import * as ping from "./ping";
import * as register from "./register";
import * as filecase from "./filecase";
import * as pendingcases from "./pendingcases";
import * as assigncase from "./assigncase";

export const commands = {
    ping, register, oath, help, filecase, pendingcases, assigncase,
    // transferpetition, noa, help
};