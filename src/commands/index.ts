import * as ping from "./ping";
import * as register from "./register";
import * as filecase from "./filecase";
import * as pendingcases from "./pendingcases";

export const commands = {
    ping, register, filecase, pendingcases
};